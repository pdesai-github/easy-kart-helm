apiVersion: apps/v1
kind: Deployment
metadata:
  name: easy-kart-media
  namespace: easy-kart
spec:
  replicas: 1
  selector:
    matchLabels:
      app: easy-kart-media #This should be same label
  template:
    metadata:
      labels:
        app: easy-kart-media #This should be same label
    spec:
      containers:
      - name: easy-kart-media
        image: pdesaidocker/easy-kart-api-media:231
        resources:
          requests:
            memory: "128Mi"   # Minimum memory for the container to function
            cpu: "25m"       # Minimum CPU for the container to function
          limits:
            memory: "256Mi"   # Maximum memory that can be used
            cpu: "50m"       # Maximum CPU that can be used
        ports:
        - containerPort: 8080 #port defined in image dockerfile
        env:
        - name: StorageConnectionString
          valueFrom:
            secretKeyRef:
              name: easy-kart-secret  
              key: storageauthkey
---
apiVersion: v1
kind: Service
metadata:
  name: easy-kart-media-service
  namespace: easy-kart
spec:
  selector:
    app: easy-kart-media #This should be same label
  ports:
    - protocol: TCP
      port: 80 #Other pods in the cluster can connect to this service on port 80
      targetPort: 8080 #port defined in image dockerfile
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: easy-kart-media-ingress
  namespace: easy-kart
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /api/images/$2
    nginx.ingress.kubernetes.io/use-regex: 'true'
    ingress.class: "nginx"
spec:
  ingressClassName: nginx
  rules:
    - host: easy-kart.pdtechhub.in
      http:
        paths:
          - path: /images(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: easy-kart-media-service
                port:
                  number: 80
---